# Historical revisions

These files are retained only for source and change review. The current deliverables are one directory above. Do not submit the earlier combined file in `before-repair/` as the current dataset. Original raw-file checksums in that directory remain the immutability baseline.
