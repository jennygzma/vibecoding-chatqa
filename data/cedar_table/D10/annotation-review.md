# D10 annotation review

Status: In Progress. Approval pending.

The full D10 conversation and all ten questions were reviewed. Six single-hop items cover the requested filename/flow, exported scope, seven-column schema, UTC/estimated time representation, formula protection, and unrepresentable date behavior. The preference labels apply to explicit requested download behavior and scope. Knowledge-fact labels describe the schema, date representation, and formula escaping.

Four multihop items combine invalid/first-valid/conflicting duplicate facts, calculate the fractional remainder beyond D3's whole-second Played reading, compare D6's saved preference with D10's downloaded file, and explain different export-button states when both lists show no rows but have different retained totals. Two items require evidence from separate original conversations. Temporal labels concern finish-time representation and actual duration precision, not implementation ordering. No open-domain question was added simply to fill a feature quota.

The first planning response mistakenly referred to eight columns and plan mode; the original request and actual implementation specify seven columns and Act. These original messages remain unchanged. Annotations use the actual schema and checked behavior rather than the mistaken planning statement. The retained-duplicate test uses an estimated timestamp fixture; the browser-downloaded real record has finished_at_estimated=false. These are distinct evidence cases.

D10 follow-up requests were submitted 70.407 and 84.285 seconds after the previous responses. They followed actual downloaded-file inspection, browser checks, source/test review and composition. Some independent review occurred while implementation was running. Original timestamps, IDs, roles and session boundaries remain intact.

The finished export now spans D1–D10 with 100 questions. Its eight category proportions differ from the supplied sample by at most 0.5 percentage points. All questions and their exact evidence were reviewed; approval remains pending.
