{
  "name": "zero-to-one",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A cozy, click-to-walk startup simulator.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test tests/*.test.js",
    "test:browser": "node tests/browser-smoke.js"
  },
  "engines": { "node": ">=18" }
}