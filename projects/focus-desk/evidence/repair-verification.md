Status: In Progress. All four recheck findings have been addressed; approval is still pending. The reviewed changes are included in this project and its dataset directory.

1. **Complete conversation projection:** Restored all 44 previously omitted public progress replies. The same four sessions now contain 69 messages, including seven later D2 repair and verification messages. Source maps retain original IDs, roles, text, locations, timestamps, and checksums. Every run-log record is accounted for as an included message or in a cleaning ledger. Internal continuation summaries are separately archived and explicitly excluded from public dialogue. All original raw files still match the pre-repair checksum manifest.
2. **Title snapshot at session end:** The original D2 conversation changed early-finish, cancellation, automatic completion, and completion-on-pause paths to resolve the task's current title when ending. A deleted task falls back to the active session's stored title. Previously finished records remain unchanged. The browser retest confirmed rename before finish, rename after finish with refresh, cancellation after rename, and finishing after deletion; all three saved records survived refresh and Insights immediately reflected three sessions. See [the repaired snapshot](screenshots/repaired-title-snapshot.png) and [the original reproduction](screenshots/review-title-snapshot.png).
3. **Grounded annotation categories:** Retained 60 questions after correcting direct-lookups, rewriting unsupported cross-session questions, replacing a redundant ordering question, and adding direct restored-message support for planned workload. Q016 now distinguishes the requested behavior, initial mismatch, and later D2 correction. The set has 12 multi-session and 17 multihop questions; reference proportions are documented as comparisons rather than quotas. Every question has a separate source/category rationale tied to its content checksum.
4. **Exact session starts:** Combined session dates now use the original `session.json.started_at` values. First-message times remain separate in source maps and the session index. The validator checks the recorded starts directly rather than equating them to first-message times.

Verification:

- All 31 local app tests passed, with zero skips or failures; syntax checks passed.
- Dataset validation passed for four distinct sessions, 69 messages, 60 questions, raw checksums, exact excerpts, complete progress coverage, exclusion ledgers, old-to-new ID mapping, preserved session configuration, and content-review version checksums.
- All six dataset-integrity tests passed. They exercise the valid archive and reject missing progress messages, substituted start times, missing ledger entries, fabricated quotes, and stale question reviews.
- The browser name-snapshot test passed on a fresh local origin. Automatic completion was covered by deterministic tests and inspection of the application end path; no claim is made that a full 25-minute browser timer was waited out.

Current data: `../../../data/focus_desk/vibe_combined.json` and `../../../data/focus_desk/annotations.json`. Supporting records: `recording-policy.md`, `session-index.json`, `message-renumbering.json`, `timing-review.md`, `annotation-semantic-review.json`, and `category-review.md` in that data directory. Previous outputs are retained under `revisions/before-repair/` for traceability and are not the current submission.

The earlier live hidden-page transition remains a browser coverage limitation; timing/recovery behavior is covered by source checks and tests. This does not change the resolution of the four recheck findings. Final review approval remains pending.
